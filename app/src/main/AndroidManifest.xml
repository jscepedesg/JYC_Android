<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sebastian.ing.jyc">

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Inicio">
        </activity>
        <activity android:name=".Pedido">
        </activity>
        <activity android:name=".Estadistica">
        </activity>
        <activity android:name=".Facturar">
        </activity>
        <activity android:name=".Cliente_Int">
        </activity>
        <activity android:name=".Cliente_lista_clientes">
        </activity>
        <activity android:name=".Cliente_lista_clientes_detalle">
        </activity>

    </application>

</manifest>